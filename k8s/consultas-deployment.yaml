apiVersion: apps/v1
kind: Deployment
metadata:
  name: consultas-deployment
  labels:
    app: consultas
spec:
  replicas: 2
  selector:
    matchLabels:
      app: consultas
  template:
    metadata:
      labels:
        app: consultas
    spec:
      containers:
      - name: consultas
        image: <TU_ACR>.azurecr.io/consultas:latest
        ports:
        - containerPort: 8000
        env:
        - name: PORT
          value: "8000"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: consultas-service
spec:
  type: ClusterIP
  selector:
    app: consultas
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
